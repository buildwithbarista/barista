name: Release

on:
  push:
    tags: ['v*']

permissions:
  contents: write

jobs:
  build:
    name: Build release artifacts (skeleton — full reproducible pipeline lands later)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Release pipeline not yet implemented; see the build-pipeline tasks."
