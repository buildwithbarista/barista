# TGF ground-truth for the resolver golden test.
#
# Hand-authored to match `mvn dependency:tree -DoutputType=tgf -Dscope=compile`
# on slf4j-api:2.0.16 walked from a synthetic root.  slf4j-api has no
# compile-scope transitive deps in upstream, so the resulting graph is
# just the root edge.
#
# Format: `<id> <group>:<artifact>:<version>:<scope>` per node.
1 org.slf4j:slf4j-api:2.0.16:compile
#
