//! Remote Execution API (REAPI) gRPC handler — placeholder.
//
// T4: REAPI gRPC handler — `tonic`-backed `ContentAddressableStorage`
// + `Capabilities` services merged into the main axum router via
// `Router::merge`. Intentionally empty until that task lands.
