# Barista CLI output schemas — v1

This directory hosts the published JSON Schemas for `barista` CLI
command outputs (the structured `--output json` envelope and the
`--output ndjson` event stream). Concrete schemas (`pull.json`,
`grind-tree.json`, `verify.json`, `progress-event.json`, …) land
alongside the CLI surface in a subsequent release.
